# MobileApp AWS Project

Stack: React Native, Go + Gin, Keycloak, PostgreSQL, GitLab CI, AWS/EKS + Helm + Terraform.

## Local dev
See infra/docker/docker-compose.yml, run:
```bash
cd infra/docker
docker compose up --build# mobileapp-aws
